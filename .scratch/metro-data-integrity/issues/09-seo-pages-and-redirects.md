# 09 — SEO 路線頁與 301 重導

**What to build:** ticket 07 讓新加坡與泰國的路線不再有可驗證資料，預渲染的路線頁也應隨之不再產生。但這些 URL 已經上線，直接消失會變成軟性 404。

範圍：新加坡與泰國共 8 條路線 × 6 個語系 = 48 個頁面。

依專案慣例，重導產生器**不在 build 流程內**（build 期間才寫入會來不及影響路由），必須手動執行並提交結果。

**Blocked by:** 07

**Status:** ready-for-agent

- [ ] 路線頁產生器不再為無可驗證資料的路線產出頁面
- [ ] 網站地圖與路線頁一致，不再列出已不存在的頁面
- [ ] 為消失的 48 個頁面產生並提交 301 重導
- [ ] 重導指向合理的目標（該國家頁或路線總覽頁），不是首頁
- [ ] 既有的手寫 sitemap 重導未被覆寫
- [ ] `npm run lint` 綠燈
